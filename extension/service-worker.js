const DEFAULT_API_URL='https://drix-connect.up.railway.app';
chrome.runtime.onInstalled.addListener(details=>{chrome.sidePanel.setPanelBehavior({openPanelOnActionClick:true}).catch(console.error);if(details.reason==='update')chrome.storage.local.clear().catch(console.error)});
chrome.runtime.onMessage.addListener((message,_sender,sendResponse)=>{const paths={DRIX_API_REQUEST:'/api/copilot/analyze',DRIX_PARTNER_RESEARCH:'/api/copilot/partner-profile'},path=paths[message?.type];if(!path)return false;chrome.storage.sync.get(['apiUrl','apiKey']).then(async settings=>{const base=(settings.apiUrl||DEFAULT_API_URL).replace(/\/+$/,'');const headers={'Content-Type':'application/json'};if(settings.apiKey)headers['x-api-key']=settings.apiKey;const response=await fetch(base+path,{method:'POST',headers,body:JSON.stringify(message.payload)});const data=await response.json().catch(()=>({error:`Server returned HTTP ${response.status}`}));if(!response.ok)throw new Error(data.error||`Server returned HTTP ${response.status}`);sendResponse({ok:true,data})}).catch(error=>sendResponse({ok:false,error:error.message}));return true});
