const DEFAULT_API_URL='https://drix-connect.up.railway.app',confirmedCompanies=new Map();
function canonical(v){try{const u=new URL(v);return u.origin+u.pathname.replace(/\/+$/,'')}catch{return v||''}}
chrome.runtime.onInstalled.addListener(details=>{chrome.sidePanel.setPanelBehavior({openPanelOnActionClick:true}).catch(console.error);if(details.reason==='update')chrome.storage.local.clear().catch(console.error)});
chrome.runtime.onMessage.addListener((message,_sender,sendResponse)=>{if(message?.type==='DRIX_CONFIRM_COMPANY'){const pageUrl=canonical(message.payload?.pageUrl),website=message.payload?.website;if(pageUrl&&website)confirmedCompanies.set(pageUrl,website);sendResponse({ok:true});return false}const paths={DRIX_API_REQUEST:'/api/copilot/analyze',DRIX_PARTNER_RESEARCH:'/api/copilot/partner-profile',DRIX_RESOLVE_COMPANY:'/api/copilot/resolve-company'},path=paths[message?.type];if(!path)return false;chrome.storage.sync.get(['apiUrl','apiKey']).then(async settings=>{const base=(settings.apiUrl||DEFAULT_API_URL).replace(/\/+$/,''),headers={'Content-Type':'application/json'},payload=structuredClone(message.payload||{});if(settings.apiKey)headers['x-api-key']=settings.apiKey;if(message.type==='DRIX_API_REQUEST'&&payload.page){const confirmed=confirmedCompanies.get(canonical(payload.page.url));if(confirmed){payload.page.companyWebsite=confirmed;payload.page.externalLinks=[confirmed];payload.page.websiteConfirmed=true}}const response=await fetch(base+path,{method:'POST',headers,body:JSON.stringify(payload)}),data=await response.json().catch(()=>({error:`Server returned HTTP ${response.status}`}));if(!response.ok)throw new Error(data.error||`Server returned HTTP ${response.status}`);sendResponse({ok:true,data})}).catch(error=>sendResponse({ok:false,error:error.message}));return true});
