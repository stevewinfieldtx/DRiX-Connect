const test=require('node:test'),assert=require('node:assert/strict');const{confidence}=require('../src/copilot');const{scoreCandidate,semanticCandidateScore}=require('../src/company-url');
const framPage={companyName:'fram^',pageTitle:'fram^ | LinkedIn',companyIdentity:{location:'Ho Chi Minh City, Vietnam',industry:'IT Services and IT Consulting',descriptor:'Digital development powerhouse building software products and managed services'}};
test('normalizes model confidence fractions to percentages',()=>{assert.equal(confidence(.95),95);assert.equal(confidence(95),95)});
test('penalizes education domains for a consulting target',()=>{const page={companyName:'EVIT',companyIdentity:{location:'Hanoi, Vietnam',industry:'IT Services and IT Consulting'}};assert.ok(scoreCandidate('https://evit-org.com',page,'EVIT Organization - AI automation','Hanoi Vietnam software')>scoreCandidate('https://evit.edu',page,'East Valley Institute of Technology','Arizona school education'))});
test('hard-rejects FRAM automotive content for the Vietnamese software company',()=>{const result=semanticCandidateScore(framPage,'FRAM oil filters protect vehicle engines for everyday drivers in Michigan and Ohio');assert.ok(result.score<=-100);assert.match(result.conflict,/automotive/)});
test('strongly accepts FRAM software content matching Vietnam and Sweden',()=>{const result=semanticCandidateScore(framPage,'fram is a Swedish-Vietnamese software development company in Ho Chi Minh City providing SaaS, DevOps and managed services');assert.ok(result.score>=60);assert.equal(result.conflict,'')});
test('ranks wearefram software evidence above fram oil-filter evidence',()=>{const good=scoreCandidate('https://wearefram.com/',framPage,'Fram software development Vietnam','Swedish Vietnamese software development and managed services'),bad=scoreCandidate('https://fram.com/',framPage,'FRAM oil filters','Automotive oil filters for vehicles and everyday drivers');assert.ok(good>bad+100)});
