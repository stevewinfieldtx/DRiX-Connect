const cheerio=require('cheerio');const BLOCKED=['linkedin.com','facebook.com','instagram.com','x.com','twitter.com','youtube.com','crunchbase.com','bloomberg.com','zoominfo.com','glassdoor.com','indeed.com','wikipedia.org','yelp.com','wsj.com'];
function httpUrl(v){try{const u=new URL(v);return['http:','https:'].includes(u.protocol)?u.href:null}catch{return null}}function unredirect(v){const url=httpUrl(v);if(!url)return null;const u=new URL(url);if(!/(^|\.)linkedin\.com$/i.test(u.hostname))return url;for(const k of['url','target','dest','destination']){const n=httpUrl(u.searchParams.get(k));if(n)return n}return null}function allowed(v){const url=unredirect(v)||httpUrl(v);if(!url)return null;const h=new URL(url).hostname.replace(/^www\./,'').toLowerCase();return BLOCKED.some(x=>h===x||h.endsWith('.'+x))?null:url}
function tokens(v){return String(v||'').toLowerCase().replace(/\b(limited|ltd|llc|inc|corp|corporation|company|co|group|holdings|plc|gmbh|the|services|consulting)\b/g,' ').replace(/[^a-z0-9]+/g,' ').trim().split(/\s+/).filter(x=>x.length>2)}
function scoreCandidate(url,pageOrName={},title='',snippet=''){const page=typeof pageOrName==='string'?{companyName:pageOrName}:pageOrName;const host=new URL(url).hostname.replace(/^www\./,'').toLowerCase(),hay=`${title} ${snippet}`.toLowerCase();let score=0;for(const t of tokens(page.companyName)){if(host.includes(t))score+=8;if(hay.includes(t))score+=2}for(const t of tokens(`${page.companyIdentity?.location||''} ${page.companyIdentity?.industry||''}`)){if(hay.includes(t))score+=2}if(/official|homepage/i.test(title))score+=2;if(/\.edu($|\/)/i.test(url)&&!/education|school|institute|training/i.test(page.companyIdentity?.industry||''))score-=12;return score}
async function discoverOfficialWebsite(page){if(!page?.companyName)return null;const id=page.companyIdentity||{},q=encodeURIComponent(`"${page.companyName}" "${id.location||''}" ${id.industry||''} official website`),r=await fetch(`https://html.duckduckgo.com/html/?q=${q}`,{signal:AbortSignal.timeout(15000),headers:{'User-Agent':'Mozilla/5.0 (compatible; DRiXResearch/1.0)'}});if(!r.ok)throw new Error(`Website discovery returned HTTP ${r.status}`);const $=cheerio.load(await r.text()),out=[];$('.result').each((_i,node)=>{const a=$(node).find('.result__a,a.result-link').first(),raw=a.attr('href');let value=raw;try{const u=new URL(raw,'https://duckduckgo.com');value=u.searchParams.get('uddg')||u.href}catch{}const url=allowed(value),title=a.text().trim(),snippet=$(node).find('.result__snippet').text().trim();if(url)out.push({url,score:scoreCandidate(url,page,title,snippet)})});out.sort((a,b)=>b.score-a.score);return out[0]?.score>=8?out[0].url:null}
async function ensureCompanyWebsite(page){const strict=[page?.companyWebsite,...(page?.externalLinks||[])].map(allowed).find(Boolean);if(strict)return{url:strict,supplied:true};try{const url=await discoverOfficialWebsite(page);return{url,discovered:!!url}}catch(e){console.warn('[copilot] company URL discovery:',e.message);return{url:null}}}
module.exports={httpUrl,unredirect,allowed,scoreCandidate,discoverOfficialWebsite,ensureCompanyWebsite};
